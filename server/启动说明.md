# 后端服务器启动说明

## 问题已修复 ✅

服务器现在可以正常启动了！

## 启动步骤

### 方法 1: 开发模式（推荐，自动重启）

```bash
cd server
npm run dev
```

### 方法 2: 生产模式

```bash
cd server
npm start
```

## 验证服务器运行

启动后，你应该看到：

```
✅ Database connected
🚀 Server running on http://localhost:3000
📝 API docs: http://localhost:3000/health
🌐 Health check: http://localhost:3000/health
```

打开浏览器访问：`http://localhost:3000/health`

应该看到：
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

## 常见问题

### 1. 端口被占用
如果 3000 端口被占用，修改 `server/.env` 文件中的 `PORT` 变量。

### 2. 数据库连接失败
- 检查 MySQL 是否运行：`mysql -u root -e "SELECT 1;"`
- 检查 `.env` 文件中的数据库配置
- 确保数据库 `shaxian_erp` 已创建

### 3. 依赖未安装
```bash
cd server
npm install
```

## 停止服务器

按 `Ctrl + C` 停止服务器。


